//Formatting:
export function getCapitalizedString(string) {
    return string.charAt(0).toUpperCase() + string.slice(1).toLowerCase();
}

export function getAllCapsString(string) {
    return string.toUpperCase();
}